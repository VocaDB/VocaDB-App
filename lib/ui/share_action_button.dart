import 'package:flutter/material.dart';
import 'package:vocadb/ui/action_button.dart';

class ShareActionButton extends ActionButton {
  const ShareActionButton({Key key}) : super(key: key, label: 'Share', icon: Icons.share);
}

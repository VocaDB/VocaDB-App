import 'package:flutter/material.dart';

class PVPlaylistPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text('Playlist')),
        body: Container(
          child: Text('PV Playlist page no implement'),
        ));
  }
}

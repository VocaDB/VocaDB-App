import 'package:flutter/material.dart';

class FavoriteArtistPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text('Favorite artists')),
        body: Container(
          child: Text('Favorite artists page no implement'),
        ));
  }
}

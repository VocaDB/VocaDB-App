import 'package:flutter/material.dart';

class FavoriteSongPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text('Favorite songs')),
        body: Container(
          child: Text('Favorite songs page no implement'),
        ));
  }
}

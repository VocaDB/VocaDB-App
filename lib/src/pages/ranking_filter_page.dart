import 'package:flutter/material.dart';

class RankingFilterPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('Ranking filter'),
        ),
        body: Container(
          child: Text('Ranking page no implement'),
        ));
  }
}

import 'package:flutter/material.dart';

class FavoriteArtistFilterPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text('Favorite artists filter')),
        body: Container(
          child: Text('Favorite artists filter page no implement'),
        ));
  }
}

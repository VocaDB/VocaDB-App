import 'package:flutter/material.dart';

class ReleaseEventDetailPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text('Event detail')),
        body: Container(
          child: Text('Event detail page no implement'),
        ));
  }
}

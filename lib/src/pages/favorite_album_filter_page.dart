import 'package:flutter/material.dart';

class FavoriteAlbumFilterPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text('Favorite albums filter')),
        body: Container(
          child: Text('Favorite albums filter page no implement'),
        ));
  }
}

import 'package:flutter/material.dart';

class FavoriteAlbumPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text('Favorite albums')),
        body: Container(
          child: Text('Favorite albums page no implement'),
        ));
  }
}

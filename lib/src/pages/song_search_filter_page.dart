import 'package:flutter/material.dart';

class SongSearchFilterPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text('Filter')),
        body: Container(
          child: Text('Song filter search page no implement'),
        ));
  }
}

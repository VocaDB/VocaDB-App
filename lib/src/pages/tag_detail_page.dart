import 'package:flutter/material.dart';

class TagDetailPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text('Tag detail')),
        body: Container(
          child: Text('Tag detail page no implement'),
        ));
  }
}

import 'package:flutter/material.dart';

class RecentAlbumSection extends StatelessWidget {
  const RecentAlbumSection({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('Recent albums section'),
    );
  }
}
